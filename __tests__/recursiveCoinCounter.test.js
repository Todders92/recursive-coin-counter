import { coinCounter } from '../src/recursiveCoinCounter.js';

describe('', () => {

  test('should return number of quarters', () => {
    var coins = coinCounter();
    expect(coins).toEqual();
  });

});